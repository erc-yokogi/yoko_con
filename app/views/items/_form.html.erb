  <%= form_for([@contract, @contract.items.build]) do |f| %>

      <%= render 'shared/error_messages', object: f.object %>


      <table>
       <tr>
        <td width = '75'>
        haikibutsu</td><td><%= f.text_field :item %></td>
       </tr>
       <tr>
        <td width = '75'>
        suryo</td><td><%= f.text_field :suryo %></td>
       </tr>
       <tr>
        <td width = '75'>
        tanka</td><td><%= f.text_field :tanka %></td>
       </tr>
      </table>

    <p>
      <%= f.submit %>
    </p>

  <% end %>